;important nao pode haver espa�os isso pode criar erros
publico=public
público=public
pública=public
publica=public
public=public


class=class
classes=class
classe=class

extends=extends
extend=extends
estender=extends

implements=implements
implementa=implements
implemments=implements

abstrato=abstract
abstract=abstract

método=metodo
metodo=metodo
method=metodo

assert=assert

boolean=boolean

break=break

byte=byte

case=case

catch=catch

char=char

const=const

continue=continue

default=default

do=do

double=double

else=else

enum=enum

'false'='false'

final=final

finally=finally

float=float

for=for

goto=goto

if=if

import=import

instanceof=instanceof

int=int

interface=interface

long=long

native=native

new=new

'null'='null'

package=package

private=private

protected=protected

return=return

short=short

static=static

strictfp=strictfp

super=super

switch=switch

synchronized=synchronized

this=this

throw=throw

throws=throws

transient=transient

'true'='true'

try=try

void=void

volatile=volatile

while=while
